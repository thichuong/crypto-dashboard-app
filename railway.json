{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload run:app"
  }
}
